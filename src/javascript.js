const common = require('./common');

module.exports = {
	extends: [
		...common.extends
	],
	plugins: [
		...common.plugins
	],
	rules: {
		...common.rules

		// Possible Errors

		// Best Practices

		// Variables

		// Node

		// Stylistic
	}
};
